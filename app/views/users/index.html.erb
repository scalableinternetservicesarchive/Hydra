<h1>Users</h1>

Our twitter has <%= User.count %> users!
<h2>Register now!</h2>
<%= link_to "New User", new_user_path %>

<h2>User List</h2>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.Username, user %><br>
      <%= user.About %><br>
      <ul>
        <%= render Post.where(Username: user.Username) %>
      </ul>
    </li>
  <% end %>
</ul>